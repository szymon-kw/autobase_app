spring:
  datasource:
    url: jdbc:mysql://localhost:3306/autobase
    username: root
    password: admin
  liquibase:
    contexts: prod
  web:
    resources:
      static-locations: classpath:/static/,file:///C:/uploads/
app:
  storage:
    location: /Users/DOM/uploads